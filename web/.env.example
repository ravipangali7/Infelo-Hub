VITE_API_BASE_URL=http://localhost:8000/api
# Canonical public origin for meta tags (production URL when building for staging)
# VITE_SITE_ORIGIN=https://infelohub.infelogroup.com
# Base URL of the Django API without /api — used for /sitemap.xml and /share/* link previews
# VITE_SHARE_BASE_URL=http://localhost:8000
